# ML Service Isolated Requirements (divergent from root requirements.txt)
# Purpose: Provide explicit, auditable list for ML image builds.
# NOTE: Hashes not included here to avoid accidental drift; generate with:
#   pip-compile --generate-hashes --output-file requirements-ml.lock requirements-ml.in
# Or using pip-tools style input file. This repo currently uses direct pins for simplicity.

# Core ML Stack
torch==2.5.1          # CPU build (see Dockerfile for index override)
torch-geometric==2.6.1
transformers==4.46.3
sentence-transformers==3.3.0

# Classical & Gradient Boosting
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
catboost==1.2.7

# Statistical / Time Series / Analytics
statsmodels==0.14.4
hmmlearn==0.3.2
empyrical==0.5.5
quantstats==0.0.62
backtesting==0.3.3

# Data Handling
pandas==2.2.3
numpy==1.26.4

# Optimization / Experiment Tracking
optuna==4.5.0
mlflow==2.18.0

# External Integrations & Infra
redis==5.2.1
pulsar-client==3.5.0
sqlalchemy==2.0.25
alembic==1.13.1
asyncpg==0.30.0
httpx==0.27.2
fastapi==0.109.0
uvicorn[standard]==0.27.0
structlog==24.1.0
prometheus-client==0.21.0

# Feature Engineering / TA (using pure-Python libraries; TA-Lib removed to avoid native build)

# Model Utilities
psutil==5.9.8

# LLM Interaction
ollama==0.4.4

# Keep in sync with Dockerfile -- update both when changing versions.
